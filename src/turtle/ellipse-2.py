# Ellipse (타원-2)
# python src/turtle/ellipse-2.py
from turtle import *

bgcolor("green")
fillcolor("magenta")

shape("circle")
shapesize(9, 16, 2) # 너비(stretch_width), 길이(stretch_length), 외곽선(outline)

done()
